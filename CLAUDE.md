## CRITICAL RULES
### At the start of the correction
- Be sure to start considering in Plan mode, get the agreement on the contents, and start the modification

### Git Commit / Push Policy - ABSOLUTELY REQUIRED
- **NEVER commit or push without explicit user approval**
- Always present changes and commit message together in a single response
- Wait for user confirmation before executing git commands
- Workflow:
  1. Show code changes AND proposed commit message together
  2. Wait for user's "OK" or approval
  3. Only then execute git add, commit, and push

## PR & Commit Language Settings

- PR titles and descriptions must be written consistently
- Commit messages must follow a defined 
