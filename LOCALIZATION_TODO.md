# Localization TODO - 両面カードの名称形式について

## 未確認事項
- [ ] 両面カードや分割カードの名称区切り文字（デリミタ）の形式。
  - 現状のコード（`card_logic.py`）では、一部で `///` から `//` への置換が行われています。
  - `translation.json` 内では `//` や ` + ` が使われている形跡があります。
  - ScryfallやMTGAの内部データ形式が更新された際、これらの区切り文字が変更される可能性があるため、注意が必要です。

## 実装上の注意
- 表示上の日本語名には `localization.get_display_card_name` を使用し、内部ロジック（検索・比較）には必ずオリジナルの `card["name"]` を使用すること。
- UI側で `iid=count` を維持し、クリックイベントではこのインデックスを用いてデータリストから直接カードを特定することで、名称の形式不一致によるバグを回避すること。
