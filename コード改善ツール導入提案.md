AI SETUP INSTRUCTIONS
TARGET: unrealities/MTGA_Draft_17Lands
LANGUAGE: Python
GOAL: Safe and consistent AI-driven development

--------------------------------------------------
1. FORMATTER
--------------------------------------------------

TOOL:
Black

INSTALL:
pip install black

USAGE:
black .

RULES:
- Do not manually format code
- Always rely on Black output

--------------------------------------------------
2. LINTER
--------------------------------------------------

TOOL:
Ruff

INSTALL:
pip install ruff

CONFIG FILE:
pyproject.toml

CONFIG CONTENT:
[tool.ruff]
select = ["E", "F", "I", "B"]
ignore = ["E501"]

USAGE:
ruff .

RULES:
- Fix all Ruff warnings before commit
- Prefer fixing code over ignoring rules

--------------------------------------------------
3. IMPORT ORDER
--------------------------------------------------

HANDLED BY:
Ruff (I rules)

RULES:
- Do not manually reorder imports
- Let Ruff manage ordering

--------------------------------------------------
4. TYPE CHECKING (GRADUAL)
--------------------------------------------------

TOOL:
mypy

INSTALL:
pip install mypy

CONFIG FILE:
pyproject.toml

CONFIG CONTENT:
[tool.mypy]
ignore_missing_imports = true
check_untyped_defs = false

RULES:
- Add type hints only to new or modified code
- Do not refactor entire files just to satisfy mypy

--------------------------------------------------
5. PRE-COMMIT GUARD
--------------------------------------------------

TOOL:
pre-commit

INSTALL:
pip install pre-commit

CONFIG FILE:
.pre-commit-config.yaml

CONFIG CONTENT:
repos:
  - repo: https://github.com/psf/black
    rev: 24.1.1
    hooks:
      - id: black
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.1.13
    hooks:
      - id: ruff

ACTIVATE:
pre-commit install

RULES:
- Never bypass pre-commit hooks

--------------------------------------------------
6. AI DEVELOPMENT RULES
--------------------------------------------------

- Do not change behavior unless explicitly instructed
- Prefer small, localized changes
- Do not manually reformat files
- When errors occur:
  1. Fix Ruff errors first
  2. Then fix runtime errors
